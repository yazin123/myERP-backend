<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #4a90e2;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .content {
            padding: 20px;
            background-color: #f9f9f9;
        }
        .footer {
            text-align: center;
            padding: 20px;
            font-size: 12px;
            color: #666;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4a90e2;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>New Project Created</h1>
        </div>
        <div class="content">
            <h2>Hello <%= user.name %>,</h2>
            <p>A new project has been created and you have been assigned as a <%= project.projectHead.equals(user._id) ? 'Project Head' : 'team member' %>.</p>
            
            <h3>Project Details:</h3>
            <ul>
                <li><strong>Project Name:</strong> <%= project.name %></li>
                <li><strong>Description:</strong> <%= project.description %></li>
                <li><strong>Start Date:</strong> <%= new Date(project.startDate).toLocaleDateString() %></li>
                <li><strong>Expected End Date:</strong> <%= new Date(project.expectedEndDate).toLocaleDateString() %></li>
                <li><strong>Tech Stack:</strong> <%= project.techStack.join(', ') %></li>
            </ul>

            <a href="<%= process.env.FRONTEND_URL %>/projects/<%= project._id %>" class="button">View Project</a>
        </div>
        <div class="footer">
            <p>This is an automated message from the ERP System. Please do not reply to this email.</p>
        </div>
    </div>
</body>
</html> 